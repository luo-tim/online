<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
 "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
 <mapper namespace="recruitSystem.dao.news.NewsDAO">
 
 <!-- 建立sql查询结果字段与实体属性的映射关系 -->
 <resultMap type="recruitSystem.view.Information" id="NewsMap">
 <result column="id" property="id" />
 <result column="context" property="context" />
 <result column="post_time" property="postTime" />
 <result column="send_id" property="sendId" />
 <result column="receive_id" property="receiveId" />
 </resultMap>
  
 <!-- 通过发送、接收者查找消息 -->
 <select id="selectBySendnReceive" resultMap="NewsMap">
 	SELECT *
 	FROM news
 	where 
 		send_id = #{sendId}
 	AND	receive_id = #{receiveId}
 </select>
 
 <!-- 插入消息 -->
 <insert id="insert">
 	INSERT INTO news(context,post_time,send_id,receive_id)
 	VALUES(#{context},#{postTime},#{sendId},#{receiveId})
 </insert>
 
 </mapper>